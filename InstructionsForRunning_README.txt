Running the Program:

Open Command Prompt/Terminal:


Navigate to the Script's Directory:

    Use the cd command to navigate to the folder containing DavidBeck_VRP.py.
    Example: cd path/to/folder/containing/script
    Run the Script with the Input File:

Execute the script by passing the path to the problem text file as an argument.

    Command format: python3 DavidBeck_VRP.py path/to/problem_file.txt
    
    Example: ❯ python3 DavidBeck_VRP.py TrainingProblems/problem1.txt

    Output Example for the above training problem:
    [8, 1]
    [6, 7]
    [10, 2]
    [5, 9]
    [4, 3]